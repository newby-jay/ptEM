# Particle Tracking Expectation-Maximization tools (ptEM)
Tools for using the EM algorithm on particle tracking data (position-time series data). The code is written in pure Python with Numba for performance.
